import { Text } from "react-native";
import React from "react";
import ScreenView from "@/components/ScreenView";

const liked = () => {
  return (
    <ScreenView>
      <Text className="text-2xl font-bold">Liked Screen</Text>
    </ScreenView>
  );
};

export default liked;
